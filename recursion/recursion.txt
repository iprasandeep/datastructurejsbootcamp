what is recursion

a process(a funtion in our case) that calls itself


-> It's everywhere
JSON.parse/JSON.sringify

document.getElementById and DOM traversal algorithms

Object traversal

We will see it with more complex data structures
It's sometimes a cleaner alternatve to iteration



